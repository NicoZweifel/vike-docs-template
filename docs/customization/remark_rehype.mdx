---
title: remark/rehype
---

This template is using a set of common `rehype/mark` plugins, including [`rehype-prism-plus`](https://www.npmjs.com/package/rehype-prism-plus) and [`prism-themes`](https://github.com/PrismJS/prism-themes).

```ts {7,10} showLineNumbers
const options: BundleMDXOptions = {
  // other options
  mdxBundlerOptions: {
    mdxOptions: {
      jsxImportSource: 'preact',
      remarkPlugins: [
        /* add or change remark plugins*/
      ],
      rehypePlugins: [
        /* add or change rehype plugins*/
      ],
    },
  },
};

export default options;
```
